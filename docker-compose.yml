services:

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile

    container_name: datingapp-backend

    ports:
      - "5000:5000"

    env_file:
      - ./backend/.env

    volumes:
      - ./backend:/app
      - /app/node_modules

    restart: always